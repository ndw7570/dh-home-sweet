Django==5.0.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
drf-spectacular==0.27.2
psycopg[binary]==3.2.1
python-dotenv==1.0.1

# ── 워크스트림 B: 실시간 시세 수집·주문 (docs/kis-stack-plan.md) ──
# 지금 phase 1 에서는 뼈대만 세운다. 실제 KIS 연동·주문 로직은 이후 phase 에서.
celery==5.4.0
redis==5.0.8
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
django-celery-beat==2.7.0
websockets==13.0.1
# KIS REST 호출용. 타임아웃을 연결/읽기로 나눠 줄 수 있어 requests 대신 httpx 를 쓴다 —
# 장 시작 직후 KIS 응답이 느려질 때 연결은 살아 있는데 읽기만 지연되는 경우를 구분해야 한다.
httpx==0.28.1
