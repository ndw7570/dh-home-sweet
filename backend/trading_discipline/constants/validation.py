"""이행 입력 sanity 임계값.

실제로 난 사고에서 나온 값들이다. 현대자동차(현재가 450,500) 에 20주 매수를 넣으면서
수량 칸에 464,000 을, 지정가격 칸에 20 을 적은 이행이 아무 저항 없이 저장됐고,
그 종목 보유수량이 464,026 으로 나갔다. 필드 매핑도 소프트딜리트도 정상이었다 —
막을 것이 아무것도 없었을 뿐이다.

임계값은 넉넉하게 잡았다. 여기 걸리는 입력은 "드문 주문" 이 아니라 "거의 확실한 오타" 여야 한다.
그래도 사람이 맞다고 하면(`confirm_outlier=true`) 통과시킨다. 판단까지 뺏지는 않는다.
"""

from decimal import Decimal

# 지정가격이 종목 현재가의 1/10 미만이거나 10배 초과면 가격 자리에 엉뚱한 값이 들어온 것으로 본다.
# (현대차 사고: 현재가 450,500 에 지정가격 20 → 1/22,525 배)
PRICE_OUTLIER_RATIO = Decimal(10)

# 수량이 종목 현재가와 이 오차 안이면 "수량 칸에 가격을 적었다" 로 본다.
# (현대차 사고: 현재가 450,500 에 수량 464,000 → 3% 차이)
QUANTITY_LOOKS_LIKE_PRICE_TOLERANCE = Decimal("0.3")

# 현재가를 모르는 종목(current_price 가 빈 신규 종목) 에서 쓰는 수량 상한.
# 개인 계좌의 단일 이행이 이 수량을 넘을 일은 없다고 보고, 넘으면 오타로 본다.
MAX_PLAUSIBLE_QUANTITY = 1_000_000
