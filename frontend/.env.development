# 프론트: http://localhost:9070 / 백엔드: http://localhost:9071
# /api 요청은 vite.config.js 의 프록시가 VITE_API_TARGET 으로 넘긴다.
VITE_API_BASE=/api/trading
VITE_API_TARGET=http://localhost:9071

# 1 이면 백엔드를 아예 안 부르고 src/data/mock.js 로 화면을 띄운다.
# 백엔드가 떠 있으면 0. (백엔드 없이 화면만 볼 때만 1 로 되돌린다)
VITE_USE_MOCK=0
